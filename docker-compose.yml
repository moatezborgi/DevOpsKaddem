version: "3.8"
services:
  mysqldb:
    image: mysql:5.7
    restart: unless-stopped
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
    ports:
      - "3307:3306"
    volumes:
      - db:/var/lib/mysql

  kaddemProject:
    depends_on:
      - mysqldb
    image: fatmamaazoun/fatmakaddem
    environment:
      MYSQL_ROOT_PASSWORD: ""
    ports:
      - "9091:9091"
    stdin_open: true
    tty: true

volumes:
  db:
